import React from 'react';

const BeltChecker = ({ color }) => {
  const checkColor = (color) => {
    if (color === 'white') {
      return;
    }
  };
  return <div>{}</div>;
};

export default BeltChecker;
